# API & HTTP
requests==2.31.0

# Data
pandas==2.2.0

# Database
# NOTE: must stay <2.0 — Airflow 2.9.1 imports `nullsfirst` which was renamed
# to `nulls_first` in SQLAlchemy 2.0. Pinning 2.0.x crashes the Airflow image.
sqlalchemy>=1.4.36,<2.0
psycopg2-binary==2.9.9

# Environment
python-dotenv==1.0.0

# Logging
loguru==0.7.2

# dbt
dbt-core==1.7.0
dbt-postgres==1.7.0
# dbt 1.7 calls MessageToJson(including_default_value_fields=...) in its end-of-run
# logging; protobuf 5+ removed that argument, which crashes `dbt build` on a fresh
# install (CI). Airflow's constraints already cap protobuf, so this is only needed
# where dbt runs without Airflow (the dbt-integration job). Pin below 5.
protobuf>=4.0.0,<5
